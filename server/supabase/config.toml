# Supabase project config (used by Supabase CLI for link + db push).
# Run: supabase link --project-ref YOUR_REF   (from server/ or with --workdir server)
# Then: supabase db push

[api]
enabled = true
port = 54321

[db]
port = 54322
major_version = 15

[studio]
enabled = true
port = 54323
